extend ../layouts/default

block main
  // TABLE
  .data-table-wrap(style="border-radius:0;border:none;border-top:1px solid var(--border);")
    .table-toolbar
      .toolbar-search
        svg(stroke-width="2" stroke="currentColor" fill="none" viewBox="0 0 24 24" height="14" width="14")
          circle(r="8" cy="11" cx="11")
          line(y2="16.65" x2="16.65" y1="21" x1="21")
        input(placeholder="Tìm sản phẩm..." type="text")

      .toolbar-actions
        select.btn-admin.btn-admin-outline
          option(value="all") Tất cả
          option(value="active") Đang bán
          option(value="hidden") Ẩn
          option(value="out") Hết hàng

    table#ordersTable.data-table
      thead
        tr
          th Mã đơn
          th Khách hàng
          th Ngày đặt
          th Sản phẩm
          th Tổng tiền
          th Trạng thái
          th Thao tác
      tbody#ordersTableBody
        tr
          td.td-bold #ORD-1284
          td
            .td-name
              .td-avatar TL
              | Trần Linh
          td 14/04/2024
          td Sofa góc L Velvet
          td.td-bold 56.800.000 ₫
          td
            .status-badge.status-active Đang hoạt động
          td
            .action-btns
              button.action-btn.action-btn-view(
                onclick="openModal('orderDetailModal')"
                title="Xem chi tiết"
              ) 👁
              button.action-btn.action-btn-delete(title="Xóa đơn") 🗑

  // PAGINATION (tách riêng)
  .table-pagination
    span Hiển thị 1–5 / 1.284 đơn hàng
    .pagination-btns
      button.page-num.active 1
      button.page-num 2
      button.page-num 3
      span(style="padding:0 6px;color:var(--text-light);font-size:0.85rem;") ...
      button.page-num 257
  // MODAL
  #orderDetailModal.modal-backdrop
    .modal-box
      .modal-header
        h3 Chi tiết đơn hàng #ORD-1284
        button.modal-close(onclick="closeModal('orderDetailModal')") ✕

      div(style="display:flex;flex-direction:column;gap:16px;")
        div(style="padding:16px;background:var(--off-white);border-radius:var(--radius-md);")
          div(style="display:grid;grid-template-columns:1fr 1fr;gap:12px;font-size:0.85rem;")
            div
              span(style="color:var(--text-light);") Khách hàng:
              br
              strong Trần Linh
            div
              span(style="color:var(--text-light);") SĐT:
              br
              strong 0912 345 678
            div
              span(style="color:var(--text-light);") Ngày đặt:
              br
              strong 14/04/2024
            div
              span(style="color:var(--text-light);") Phương thức TT:
              br
              strong Chuyển khoản
            div(style="grid-column:1/-1;")
              span(style="color:var(--text-light);") Địa chỉ giao:
              br
              strong 123 Điện Biên Phủ, Bình Thạnh

        div
          p(style="font-size:0.78rem;font-weight:800;text-transform:uppercase;letter-spacing:0.08em;color:var(--text-light);margin-bottom:10px;") Sản phẩm
          div(style="display:flex;flex-direction:column;gap:10px;")
            div(style="display:flex;align-items:center;gap:12px;padding:10px;border:1px solid var(--border);border-radius:var(--radius-sm);")
              div(style="width:44px;height:44px;background:var(--light-bg);border-radius:6px;display:flex;align-items:center;justify-content:center;font-size:1.4rem;") 🛋
              div(style="flex:1;font-size:0.85rem;")
                strong Sofa góc L Velvet Teal
                br
                span(style="color:var(--text-light);") Màu: Xanh ngọc • SL: 1
              strong(style="white-space:nowrap;") 56.800.000 ₫

        div(style="border-top:1px solid var(--border);padding-top:12px;display:flex;justify-content:space-between;")
          strong Tổng thanh toán
          strong(style="color:var(--primary);") 57.950.000 ₫